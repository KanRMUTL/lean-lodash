import { Route } from "../types/route";
import Home from "../components/pages/Home";

const paths: Route[] = [
  {
    label: "Home",
    path: "/",
    component: Home,
  },
];

export default paths;
